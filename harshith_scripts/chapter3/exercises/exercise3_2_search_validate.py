# exercise3_2_search_validate.py

# A mock database of records
records = {
    101: "User profile details",
    102: "Payment gateway options",
    103: "Critical system event log containing key information"
}

# TODO: Implement get_record(record_id: int) -> tuple[str, bool]
# - If the record_id is not in the records dict keys, return a warning string and False.
# - Otherwise, return the record value and True.
def get_record(record_id: int) -> tuple[str, bool]:
    pass
